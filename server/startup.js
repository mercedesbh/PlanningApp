Meteor.startup(function() {
    // Meteor.users.remove({});

    // Tasks.remove({});
    // Tasks.insert({});

    // Goals.remove({});
    // Goals.insert({});

    // Texts.remove({});
    // Texts.insert({});

})
